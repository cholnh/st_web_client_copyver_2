import 'package:flutter/material.dart';

class OrderInfoHistoryWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(20.0),
      child: Center(
          child: Text('과거 주문내역이 없습니다.')
      ),
    );
  }
}
